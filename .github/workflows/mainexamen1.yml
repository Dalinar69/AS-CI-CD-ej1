name: ej1-workflow

on: 
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

        - name:  Descargar codigo
          uses: actions/checkout@v4

        - name: Ejecutar comandos
          run: |
                pip install pytest
                pytest test.py
