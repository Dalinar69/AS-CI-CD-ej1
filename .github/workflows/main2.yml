name: linter

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - name: linterejecucion
        uses:  advanced-security/python-lint-code-scanning-action@v1
        with:
          linter: pylint
  
